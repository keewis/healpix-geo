version: 2

build:
  os: ubuntu-lts-latest
  tools:
    # just so RTD stops complaining
    python: "latest"
  apt_packages:
    - clang
  jobs:
    post_checkout:
      - (git --no-pager log --pretty="tformat:%s" -1 | grep -vqF "[skip-rtd]") || exit 183
      - git fetch --unshallow || true
    create_environment:
      - asdf plugin add pixi
      - asdf install pixi latest
      - asdf global pixi latest
    pre_install:
      - git update-index --assume-unchanged python/healpix-geo/docs/conf.py
    install:
      - pixi install -e jupyterlite
      - pixi install -e templates
      - pixi install -e docs
    build:
      html:
        - pixi run build-emscripten
        - pixi run create-environment
        - pixi run build-docs-rtd

sphinx:
  fail_on_warning: true
  configuration: python/healpix-geo/docs/conf.py

formats: []
